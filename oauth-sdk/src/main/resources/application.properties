server.port=8899
spring.mvc.view.suffix=.html
server.servlet.context-path=/oauth-sdk
# We have two types of environments are 'sprint' and 'sandbox'.
# Based on the environment we can change it here!
spring.profiles.active=spring
#spring.profiles.active=sandbox

